# Pinned versions for reproducible image builds (pip install . -c constraints.txt).
# mcp >= 1.23 carries the DNS-rebinding protection (CVE-2025-66416); the sidecar
# also binds to 127.0.0.1, but the fix is kept regardless.
mcp==1.30.0
ldap3==2.9.1
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
starlette==1.6.0
uvicorn==0.53.0
cryptography==50.0.1
pyasn1==0.6.4
